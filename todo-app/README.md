## Todo app

Currently, just creates an http server on port specified by an environment variable named PORT

### How to run

- Create the docker image from the Dockerfile in the current directory.
- Import the image to your cluster.
- Deploy the imported image.
